/** How long to debounce before syncing to the cloud (ms) */
export const SYNC_DEBOUNCE_MS = 2000;

/** Maximum number of history entries to keep per deck */
export const HISTORY_CAP = 50;

/** Maximum number of recent decks to store in localStorage */
export const MAX_RECENT_DECKS = 20;

/** Number of cards to eager-load images for (rest use lazy loading) */
export const EAGER_LOAD_COUNT = 8;
